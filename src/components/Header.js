export const Header = () => {
  // const { isLoggedIn } = useAuth();
  // const { user } = useAuth();

  return (
    <div>
      <h1>Phonebook</h1>
      <button>LogIn</button>
      <button>LogOut</button>
      <button>Registration</button>
    </div>
  );
};
